import { Meta, StoryObj } from "@storybook/react";
import Main from ".";

export default {
  title: "Components/Main",
  component: Main,
  parameters: {
    layout: "fullscreen",
  },
} as Meta;

export const Default: StoryObj = {};
